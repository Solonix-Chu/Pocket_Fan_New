
void TKR() {
  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_1, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_2, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_3, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_4, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_5, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_6, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_7, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_8, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_9, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_10, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_11, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_12, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_13, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_14, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_15, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_16, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_17, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_18, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_19, 128, 64, 1);
  display.display();
  delay(fps);

  display.clearDisplay();
  display.drawBitmap(32, 0, gImage_20, 128, 64, 1);
  display.display();
  delay(fps);
}
